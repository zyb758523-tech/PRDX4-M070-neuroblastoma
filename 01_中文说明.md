# PRDX4–M070 代码与结果包中文说明

本包用于论文代码审查、结果追溯和后续上传公共代码仓库。包内保留的是已找回的原始执行脚本，不把新整理的代码冒充为当时实际运行的脚本。

## 最终有效顺序

`01 → 02 → 03审计 → 04C过渡复跑 → 05最终重复交叉验证 → 06锁定模型 → 06B外部验证尺度修正 → 07A → 07A-FIX1 → 07B-FIX3 → 07C`

- 03 文件名中的 M050 仅表示它审计了旧流程，不代表最终模型仍为 M050。
- 04C 是过渡性101模型复跑；论文最终的模型选择依据是05中的 E-MTAB-179 10次重复5折交叉验证。
- 06先锁定 M070；由于平台尺度不兼容导致 SEQC 预测为常数，06B在不查看生存结局的前提下采用 SEQC 队列内逐基因 z 标准化，并使用完全不变的锁定模型进行外部验证。
- 07A中年龄单位和DCA问题已经由07A-FIX1纠正；论文只能使用FIX1结果。
- 通路和免疫机制使用07B-FIX3，跨平台共同基因宇宙敏感性分析使用07C。

## 运行要求

原始电脑路径为：

- 输出根目录：`C:\Users\Administrator\Desktop\NB_Lactate_Rebuild_2026`
- 输入根目录：`C:\Users\Administrator\Desktop\NB_Lactate_Bulk_ServerInput_20260522_112238`

若文件仍在这些位置，可按顺序运行；若位置改变，只修改每个脚本开头的路径变量。每次都从第1行运行整个脚本，不要只运行选中的几段。R包版本见 `Session_Info` 和各阶段日志。

## 锁定模型校验

- 文件：`Models/M070_locked_model_BEFORE_SEQC.rds`
- MD5：`e719d0e6b626e75cc134e83fd0929083`
- SHA-256：`2b8aac34f99f11049252a785c2b783628defde476efd74abd117692acaff6909`

如果校验值不同，应停止运行，不能继续把该对象当作论文中的锁定M070模型。

## 投稿和公开仓库

本压缩包不建议直接作为 BMC Medical Genomics 的补充文件上传。更合适的做法是上传 GitHub/GitLab 与 Zenodo 等公共仓库，再把仓库URL和永久DOI填入正文 `Code availability`。正式公开前还需由作者选择代码许可证并确认不包含患者隐私、账号口令或非公开数据。

